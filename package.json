{
  "name": "lead-tracking-system",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de Acompanhamento de Leads com Kanban e Automação WhatsApp",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npm -w frontend run dev",
    "dev:backend": "npm -w backend run dev",
    "build": "npm -w frontend run build && npm -w backend run build",
    "build:frontend": "npm -w frontend run build",
    "build:backend": "npm -w backend run build",
    "test": "npm -w frontend run test && npm -w backend run test",
    "lint": "npm -w frontend run lint && npm -w backend run lint",
    "clean": "rm -rf node_modules frontend/node_modules backend/node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
