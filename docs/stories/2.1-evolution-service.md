# Story 2.1: Backend - Service da Evolution API

**Epic:** EPIC-2 - Integração WhatsApp
**Status:** Done
**Prioridade:** P0
**Estimativa:** 4h
**Assignee:** @dev
**Depends on:** Story 1.3

---

## Descrição

**Como** sistema
**Quero** um serviço encapsulado para a Evolution API
**Para** centralizar todas as chamadas à API externa

---

## Critérios de Aceite

- [x] Classe `EvolutionService` criada
- [x] Método `createInstance(name, config)` implementado
- [x] Método `getQRCode(instanceName)` implementado
- [x] Método `getConnectionStatus(instanceName)` implementado
- [x] Método `sendText(instanceName, number, text)` implementado
- [x] Método `sendMedia(instanceName, number, mediaConfig)` implementado
- [x] Método `setWebhook(instanceName, webhookConfig)` implementado
- [x] Método `deleteInstance(instanceName)` implementado
- [x] Tratamento de erros com retry logic
- [x] Logs de todas as operações

---

## Tasks

- [x] Criar arquivo evolution.service.ts
- [x] Criar arquivo evolution.types.ts
- [x] Implementar cliente Axios configurado
- [x] Implementar createInstance()
- [x] Implementar getQRCode()
- [x] Implementar getConnectionStatus()
- [x] Implementar sendText()
- [x] Implementar sendMedia()
- [x] Implementar setWebhook()
- [x] Implementar deleteInstance()
- [x] Adicionar retry logic para falhas
- [x] Adicionar logging em todas as operações
- [ ] Testar manualmente cada método

---

## File List

```
backend/src/services/evolution/
├── evolution.service.ts
├── evolution.types.ts
└── evolution.utils.ts
```

---

## Interface do Service

```typescript
interface EvolutionService {
  // Instâncias
  createInstance(name: string, config?: InstanceConfig): Promise<InstanceResponse>
  getQRCode(instanceName: string): Promise<QRCodeResponse>
  getConnectionStatus(instanceName: string): Promise<ConnectionStatus>
  deleteInstance(instanceName: string): Promise<void>

  // Mensagens
  sendText(instanceName: string, number: string, text: string, options?: SendOptions): Promise<MessageResponse>
  sendMedia(instanceName: string, number: string, media: MediaConfig): Promise<MessageResponse>

  // Webhook
  setWebhook(instanceName: string, config: WebhookConfig): Promise<void>
}
```

---

## Notas Técnicas

**Types (evolution.types.ts):**
```typescript
export interface InstanceConfig {
  qrcode?: boolean
  rejectCall?: boolean
  groupsIgnore?: boolean
}

export interface InstanceResponse {
  instance: {
    instanceName: string
    instanceId: string
    status: string
  }
  hash: {
    apikey: string
  }
}

export interface QRCodeResponse {
  pairingCode: string
  code: string
  count: number
}

export interface SendTextDTO {
  number: string
  text: string
  delay?: number
  linkPreview?: boolean
}

export interface SendMediaDTO {
  number: string
  mediaType: 'image' | 'video' | 'audio' | 'document'
  mimeType: string
  mediaUrl: string
  caption?: string
  fileName?: string
}

export interface MessageResponse {
  key: {
    remoteJid: string
    fromMe: boolean
    id: string
  }
  message: Record<string, any>
  messageTimestamp: string
  status: string
}
```

**Retry Logic:**
```typescript
async function withRetry<T>(
  fn: () => Promise<T>,
  retries = 3,
  delay = 1000
): Promise<T> {
  try {
    return await fn()
  } catch (error) {
    if (retries > 0) {
      await new Promise(r => setTimeout(r, delay))
      return withRetry(fn, retries - 1, delay * 2)
    }
    throw error
  }
}
```

---

## Definition of Done

- [x] Todos os métodos implementados
- [x] Retry logic funcionando
- [x] Logs aparecendo no console
- [x] Tipos TypeScript corretos
- [ ] Testado com Evolution API real

---

## Dev Agent Record

**Arquivos criados:**
- `backend/src/services/evolution/evolution.service.ts` - Service principal com todos os métodos
- `backend/src/services/evolution/evolution.types.ts` - Tipos TypeScript completos
- `backend/src/services/evolution/evolution.utils.ts` - Utilitários (retry, formatPhone, mime)
- `backend/src/services/evolution/index.ts` - Barrel export

**Métodos implementados:**
- `createInstance()` - Criar instância WhatsApp
- `getQRCode()` - Obter QR Code para conexão
- `getConnectionStatus()` - Status de conexão
- `sendText()` - Enviar mensagem de texto
- `sendMedia()` - Enviar mídia (imagem, vídeo, áudio, documento)
- `setWebhook()` - Configurar webhook
- `logout()` - Desconectar instância
- `deleteInstance()` - Deletar instância
- `fetchInstances()` - Listar todas instâncias
- `healthCheck()` - Verificar disponibilidade da API
