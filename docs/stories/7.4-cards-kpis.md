# Story 7.4: Frontend - Cards de KPIs

**Epic:** EPIC-7 - Dashboard & Analytics
**Status:** Ready
**Prioridade:** P2
**Estimativa:** 2h
**Assignee:** @dev
**Depends on:** Story 7.3

---

## Descrição

**Como** usuário
**Quero** ver métricas principais em destaque
**Para** acompanhar indicadores chave

---

## Critérios de Aceite

- [ ] Card: Total de Leads
- [ ] Card: Novos Leads (período)
- [ ] Card: Taxa de Conversão
- [ ] Card: Mensagens Enviadas
- [ ] Indicador de variação (↑ ou ↓ + %)
- [ ] Cor verde para positivo, vermelho para negativo
- [ ] Tooltip com detalhes

---

## Tasks

- [ ] Criar componente MetricCard.tsx
- [ ] Implementar indicador de variação
- [ ] Adicionar ícones
- [ ] Implementar loading state
- [ ] Adicionar tooltip
- [ ] Estilizar cores

---

## Componente MetricCard

```typescript
interface MetricCardProps {
  title: string
  value: string | number
  change?: number  // percentual
  changeLabel?: string
  icon?: ReactNode
  loading?: boolean
}

export function MetricCard({
  title,
  value,
  change,
  changeLabel,
  icon,
  loading
}: MetricCardProps) {
  const isPositive = change && change > 0
  const isNegative = change && change < 0

  if (loading) {
    return (
      <Card>
        <CardContent className="p-6">
          <Skeleton className="h-4 w-24 mb-2" />
          <Skeleton className="h-8 w-16" />
        </CardContent>
      </Card>
    )
  }

  return (
    <Card>
      <CardContent className="p-6">
        <div className="flex items-center justify-between">
          <p className="text-sm text-muted-foreground">{title}</p>
          {icon && (
            <div className="text-muted-foreground">
              {icon}
            </div>
          )}
        </div>

        <div className="mt-2 flex items-baseline gap-2">
          <p className="text-2xl font-bold">{value}</p>

          {change !== undefined && (
            <TooltipProvider>
              <Tooltip>
                <TooltipTrigger>
                  <span className={cn(
                    'text-sm flex items-center',
                    isPositive && 'text-green-600',
                    isNegative && 'text-red-600'
                  )}>
                    {isPositive ? (
                      <ArrowUp className="w-3 h-3 mr-1" />
                    ) : isNegative ? (
                      <ArrowDown className="w-3 h-3 mr-1" />
                    ) : null}
                    {Math.abs(change).toFixed(1)}%
                  </span>
                </TooltipTrigger>
                <TooltipContent>
                  <p>{changeLabel || 'Comparado ao período anterior'}</p>
                </TooltipContent>
              </Tooltip>
            </TooltipProvider>
          )}
        </div>
      </CardContent>
    </Card>
  )
}
```

---

## Uso no Dashboard

```typescript
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
  <MetricCard
    title="Total de Leads"
    value={overview.total_leads}
    change={overview.leads_change_percent}
    changeLabel="vs. últimos 30 dias"
    icon={<Users className="w-5 h-5" />}
    loading={isLoading}
  />
  <MetricCard
    title="Novos Leads"
    value={overview.leads_this_period}
    change={20}
    icon={<UserPlus className="w-5 h-5" />}
    loading={isLoading}
  />
  <MetricCard
    title="Taxa de Conversão"
    value={`${overview.conversion_rate.toFixed(1)}%`}
    change={overview.conversion_change_percent}
    icon={<TrendingUp className="w-5 h-5" />}
    loading={isLoading}
  />
  <MetricCard
    title="Mensagens Enviadas"
    value={overview.messages_sent}
    change={10}
    icon={<MessageSquare className="w-5 h-5" />}
    loading={isLoading}
  />
</div>
```

---

## Variantes de Cor

```typescript
// Cores baseadas no tipo de métrica
const cardVariants = {
  default: 'bg-white',
  primary: 'bg-blue-50 border-blue-200',
  success: 'bg-green-50 border-green-200',
  warning: 'bg-yellow-50 border-yellow-200',
  danger: 'bg-red-50 border-red-200'
}
```

---

## Definition of Done

- [ ] Cards renderizando corretamente
- [ ] Indicadores de variação
- [ ] Cores corretas (verde/vermelho)
- [ ] Tooltips funcionando
- [ ] Loading states
