# Story 2.3: Backend - Persistência de Instâncias

**Epic:** EPIC-2 - Integração WhatsApp
**Status:** Done
**Prioridade:** P0
**Estimativa:** 2h
**Assignee:** @dev
**Depends on:** Story 1.4, Story 2.1

---

## Descrição

**Como** sistema
**Quero** salvar instâncias no Supabase
**Para** manter registro e recuperar API keys

---

## Critérios de Aceite

- [x] Criar instância salva no banco
- [x] API key da Evolution salva
- [x] Status atualizado via webhook ou polling
- [x] Hard delete implementado (soft delete pode ser adicionado depois)
- [x] Query por workspace_id
- [x] Busca por instance_name

---

## Tasks

- [x] Criar whatsapp-instance.service.ts
- [x] Implementar create()
- [x] Implementar findByWorkspace(workspaceId)
- [x] Implementar findById(id)
- [x] Implementar findByName(instanceName)
- [x] Implementar updateStatus(id, status)
- [x] Implementar update(id, data)
- [x] Implementar delete(id)
- [x] Implementar isNameAvailable()

---

## File List

```
backend/src/services/
└── whatsapp-instance.service.ts
```

---

## Interface do Service

```typescript
interface WhatsAppInstanceService {
  create(data: CreateInstanceDTO): Promise<WhatsAppInstance>
  findAll(workspaceId: string): Promise<WhatsAppInstance[]>
  findById(id: string): Promise<WhatsAppInstance | null>
  findByName(instanceName: string): Promise<WhatsAppInstance | null>
  updateStatus(id: string, status: InstanceStatus): Promise<void>
  updatePhoneNumber(id: string, phone: string): Promise<void>
  updateQRCode(id: string, qrCode: string): Promise<void>
  delete(id: string): Promise<void>
}
```

---

## Notas Técnicas

**Implementação:**
```typescript
export class WhatsAppInstanceService {
  async create(data: CreateInstanceDTO): Promise<WhatsAppInstance> {
    const { data: instance, error } = await supabase
      .from('whatsapp_instances')
      .insert({
        workspace_id: data.workspace_id,
        instance_name: data.instance_name,
        api_key: data.api_key,
        api_url: config.evolution.apiUrl,
        status: 'disconnected',
      })
      .select()
      .single()

    if (error) throw new AppError('Failed to create instance', 500)
    return instance
  }

  async findAll(workspaceId: string): Promise<WhatsAppInstance[]> {
    const { data, error } = await supabase
      .from('whatsapp_instances')
      .select('*')
      .eq('workspace_id', workspaceId)
      .order('created_at', { ascending: false })

    if (error) throw new AppError('Failed to list instances', 500)
    return data || []
  }

  async updateStatus(id: string, status: InstanceStatus): Promise<void> {
    const { error } = await supabase
      .from('whatsapp_instances')
      .update({ status, updated_at: new Date().toISOString() })
      .eq('id', id)

    if (error) throw new AppError('Failed to update status', 500)
  }

  async findByName(instanceName: string): Promise<WhatsAppInstance | null> {
    const { data } = await supabase
      .from('whatsapp_instances')
      .select('*')
      .eq('instance_name', instanceName)
      .single()

    return data
  }
}
```

---

## Definition of Done

- [x] CRUD completo funcionando
- [x] Queries otimizadas
- [x] Erros tratados
- [x] TypeScript sem erros

---

## Dev Agent Record

**Arquivo criado:** `backend/src/services/whatsapp-instance.service.ts`

**Métodos implementados:**
- `create(data)` - Cria registro no banco
- `findByWorkspace(workspaceId)` - Lista por workspace
- `findById(id, workspaceId?)` - Busca por ID com validação de workspace
- `findByName(instanceName, workspaceId)` - Busca por nome
- `update(id, data)` - Atualização genérica
- `updateStatus(id, status, phoneNumber?)` - Atualiza status
- `delete(id)` - Remove registro
- `isNameAvailable(instanceName, workspaceId)` - Verifica disponibilidade
