# Story 6.7: Frontend - Notificação de Nova Mensagem

**Epic:** EPIC-6 - Webhooks & Recebimento de Mensagens
**Status:** Ready
**Prioridade:** P2
**Estimativa:** 3h
**Assignee:** @dev
**Depends on:** Story 6.2, Story 6.6

---

## Descrição

**Como** usuário
**Quero** ser notificado quando um lead responder
**Para** poder responder rapidamente

---

## Critérios de Aceite

- [ ] Toast notification quando mensagem chega
- [ ] Badge no ícone de leads com mensagens não lidas
- [ ] Card do lead no Kanban indica "nova mensagem"
- [ ] Som de notificação (opcional, configurável)
- [ ] Click na notificação abre detalhe do lead

---

## Tasks

- [ ] Criar hook useMessageNotifications.ts
- [ ] Criar componente NewMessageToast.tsx
- [ ] Criar componente UnreadBadge.tsx
- [ ] Adicionar indicador no KanbanCard
- [ ] Implementar som de notificação
- [ ] Integrar realtime global
- [ ] Testar notificações

---

## Componentes

**NewMessageToast.tsx:**
```typescript
export function NewMessageToast({ message, leadName, onClick }: Props) {
  return (
    <div
      className="flex items-center gap-3 cursor-pointer"
      onClick={onClick}
    >
      <div className="bg-green-500 rounded-full p-2">
        <MessageCircle className="w-4 h-4 text-white" />
      </div>
      <div>
        <p className="font-medium">{leadName}</p>
        <p className="text-sm text-muted-foreground truncate max-w-[200px]">
          {message}
        </p>
      </div>
    </div>
  )
}
```

**UnreadBadge.tsx:**
```typescript
export function UnreadBadge({ count }: { count: number }) {
  if (count === 0) return null

  return (
    <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs rounded-full w-5 h-5 flex items-center justify-center">
      {count > 9 ? '9+' : count}
    </span>
  )
}
```

---

## Hook useMessageNotifications

```typescript
export function useMessageNotifications() {
  const navigate = useNavigate()
  const { toast } = useToast()
  const [unreadCount, setUnreadCount] = useState(0)
  const audioRef = useRef<HTMLAudioElement | null>(null)

  useEffect(() => {
    audioRef.current = new Audio('/sounds/notification.mp3')

    const channel = supabase
      .channel('global-messages')
      .on('postgres_changes', {
        event: 'INSERT',
        schema: 'public',
        table: 'messages',
        filter: 'direction=eq.inbound'
      }, async (payload) => {
        const message = payload.new as Message

        // Buscar nome do lead
        const { data: lead } = await supabase
          .from('leads')
          .select('name')
          .eq('id', message.lead_id)
          .single()

        // Tocar som
        if (audioRef.current) {
          audioRef.current.play().catch(() => {})
        }

        // Mostrar toast
        toast({
          title: 'Nova mensagem',
          description: (
            <NewMessageToast
              message={message.content}
              leadName={lead?.name || 'Lead'}
              onClick={() => navigate(`/leads/${message.lead_id}`)}
            />
          )
        })

        // Incrementar contador
        setUnreadCount(prev => prev + 1)
      })
      .subscribe()

    return () => { channel.unsubscribe() }
  }, [])

  return { unreadCount, resetUnread: () => setUnreadCount(0) }
}
```

---

## Indicador no Kanban

```typescript
// KanbanCard.tsx
export function KanbanCard({ lead }: { lead: Lead }) {
  return (
    <div className="relative">
      {lead.has_unread_messages && (
        <div className="absolute top-2 right-2">
          <div className="w-2 h-2 bg-green-500 rounded-full animate-pulse" />
        </div>
      )}
      {/* ... resto do card */}
    </div>
  )
}
```

---

## Definition of Done

- [ ] Toast aparece com nova mensagem
- [ ] Badge mostra contagem de não lidas
- [ ] Indicador verde no card do Kanban
- [ ] Som de notificação toca
- [ ] Click navega para o lead
