# Story 6.1: Backend - Endpoint de Webhook

**Epic:** EPIC-6 - Webhooks & Recebimento de Mensagens
**Status:** Ready
**Prioridade:** P1
**Estimativa:** 3h
**Assignee:** @dev
**Depends on:** Story 2.1

---

## Descrição

**Como** Evolution API
**Quero** enviar eventos para o sistema
**Para** notificar sobre mensagens e conexões

---

## Critérios de Aceite

- [ ] `POST /api/webhooks/evolution` endpoint
- [ ] Validar origem da requisição (IP ou secret)
- [ ] Parsear payload de diferentes eventos
- [ ] Responder 200 rapidamente (processar async)
- [ ] Log de todos os eventos recebidos
- [ ] Tratamento de erros silencioso (não quebrar)

---

## Tasks

- [ ] Criar rota POST /webhooks/evolution
- [ ] Implementar validação de origem
- [ ] Criar parser de eventos
- [ ] Implementar processamento assíncrono
- [ ] Adicionar logging
- [ ] Testar com payloads simulados

---

## File List

```
backend/src/routes/
├── webhooks.ts

backend/src/services/
├── webhook.service.ts

backend/src/controllers/
├── webhook.controller.ts
```

---

## Headers Esperados

```
Content-Type: application/json
X-Webhook-Secret: {secret} (opcional)
```

---

## Payload Base

```json
{
  "event": "messages.upsert",
  "instance": "minha-instancia",
  "data": { ... }
}
```

---

## Implementação

```typescript
// webhook.controller.ts
export async function handleEvolutionWebhook(req: Request, res: Response) {
  // Responder imediatamente
  res.status(200).json({ received: true })

  // Processar async
  try {
    const { event, instance, data } = req.body

    // Log do evento
    console.log(`[Webhook] Event: ${event}, Instance: ${instance}`)

    // Rotear para handler específico
    await routeWebhookEvent(event, instance, data)
  } catch (error) {
    // Não quebrar - apenas logar
    console.error('[Webhook] Error processing:', error)
  }
}
```

---

## Validação de Origem

```typescript
function validateWebhookOrigin(req: Request): boolean {
  // Opção 1: Validar IP
  const allowedIPs = process.env.EVOLUTION_WEBHOOK_IPS?.split(',')
  if (allowedIPs && !allowedIPs.includes(req.ip)) {
    return false
  }

  // Opção 2: Validar secret
  const secret = req.headers['x-webhook-secret']
  if (secret && secret !== process.env.WEBHOOK_SECRET) {
    return false
  }

  return true
}
```

---

## Definition of Done

- [ ] Endpoint respondendo 200
- [ ] Validação funcionando
- [ ] Logs registrados
- [ ] Erros não quebram
