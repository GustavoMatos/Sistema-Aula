# Story 1.3: Setup do Backend (Node.js)

**Epic:** EPIC-1 - Setup & Infraestrutura
**Status:** Done
**Prioridade:** P0
**Estimativa:** 4h
**Assignee:** @dev
**Depends on:** Story 1.1

---

## Descrição

**Como** desenvolvedor
**Quero** o projeto Node.js configurado com Express e TypeScript
**Para** criar a API do sistema

---

## Critérios de Aceite

- [x] Express + TypeScript inicializado
- [x] Estrutura de pastas MVC definida
- [x] Supabase client configurado (service role)
- [x] Middleware de autenticação preparado
- [x] CORS configurado para frontend
- [x] Error handling global implementado
- [x] Logging configurado (Winston)
- [x] Variáveis de ambiente com dotenv
- [x] Health check endpoint funcionando
- [x] `npm run dev` inicia o servidor

---

## Tasks

- [x] Criar package.json no diretório backend/
- [x] Instalar dependências
- [x] Configurar TypeScript
- [x] Criar estrutura de pastas
- [x] Configurar Express app
- [x] Configurar middlewares (cors, helmet, rate-limit)
- [x] Configurar Supabase client
- [x] Criar middleware de autenticação
- [x] Criar error handler global
- [x] Configurar Winston logger
- [x] Criar endpoint /health
- [x] Configurar tsx para desenvolvimento

---

## Dependências NPM

```json
{
  "dependencies": {
    "express": "^4.x",
    "@supabase/supabase-js": "^2.x",
    "cors": "^2.x",
    "helmet": "^7.x",
    "express-rate-limit": "^7.x",
    "dotenv": "^16.x",
    "axios": "^1.x",
    "node-cron": "^3.x",
    "zod": "^3.x",
    "winston": "^3.x",
    "multer": "^1.x"
  },
  "devDependencies": {
    "typescript": "^5.x",
    "tsx": "^4.x",
    "@types/express": "^4.x",
    "@types/node": "^20.x",
    "@types/cors": "^2.x",
    "@types/multer": "^1.x",
    "nodemon": "^3.x"
  }
}
```

---

## File List

```
backend/
├── src/
│   ├── config/
│   │   ├── index.ts
│   │   ├── supabase.ts
│   │   └── evolution.ts
│   ├── controllers/
│   │   └── health.controller.ts
│   ├── middlewares/
│   │   ├── auth.middleware.ts
│   │   ├── error.middleware.ts
│   │   ├── validate.middleware.ts
│   │   └── rateLimit.middleware.ts
│   ├── routes/
│   │   ├── index.ts
│   │   └── health.routes.ts
│   ├── services/
│   │   └── .gitkeep
│   ├── types/
│   │   └── index.ts
│   ├── utils/
│   │   ├── logger.ts
│   │   ├── errors.ts
│   │   └── async.ts
│   ├── app.ts
│   └── index.ts
├── .env.example
├── tsconfig.json
├── nodemon.json
└── package.json
```

---

## Notas Técnicas

**Supabase Client (src/config/supabase.ts):**
```typescript
import { createClient } from '@supabase/supabase-js'
import { config } from './index'

export const supabase = createClient(
  config.supabase.url,
  config.supabase.serviceKey
)
```

**Auth Middleware (src/middlewares/auth.middleware.ts):**
```typescript
import { Request, Response, NextFunction } from 'express'
import { supabase } from '../config/supabase'

export async function authMiddleware(
  req: Request,
  res: Response,
  next: NextFunction
) {
  const token = req.headers.authorization?.replace('Bearer ', '')

  if (!token) {
    return res.status(401).json({ error: 'No token provided' })
  }

  const { data: { user }, error } = await supabase.auth.getUser(token)

  if (error || !user) {
    return res.status(401).json({ error: 'Invalid token' })
  }

  // Get user with workspace
  const { data: userData } = await supabase
    .from('users')
    .select('*, workspaces(*)')
    .eq('id', user.id)
    .single()

  req.user = userData
  next()
}
```

**Variáveis de ambiente (.env.example):**
```env
NODE_ENV=development
PORT=3001

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

EVOLUTION_API_URL=
EVOLUTION_API_KEY=

FRONTEND_URL=http://localhost:5173
```

---

## Definition of Done

- [x] Servidor rodando com `npm run dev`
- [x] Health check retornando OK
- [x] Middlewares configurados
- [x] Supabase conectando (com fallback se não configurado)
- [x] Logs funcionando
- [x] TypeScript compilando sem erros
