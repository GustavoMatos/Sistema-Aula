# Story 1.2: Setup do Frontend (React)

**Epic:** EPIC-1 - Setup & Infraestrutura
**Status:** Done
**Prioridade:** P0
**Estimativa:** 4h
**Assignee:** @dev
**Depends on:** Story 1.1

---

## Descrição

**Como** desenvolvedor
**Quero** o projeto React configurado com Vite e TypeScript
**Para** desenvolver a interface do usuário

---

## Critérios de Aceite

- [x] Vite + React + TypeScript inicializado
- [x] Tailwind CSS configurado e funcionando
- [x] shadcn/ui instalado e configurado
- [x] React Router configurado com rotas básicas
- [x] Estrutura de pastas definida
- [x] ESLint + Prettier configurados
- [x] Supabase client configurado
- [x] `npm run dev` inicia o projeto sem erros

---

## Tasks

- [x] Criar projeto com `npm create vite@latest frontend -- --template react-ts`
- [x] Instalar e configurar Tailwind CSS
- [x] Instalar e configurar shadcn/ui
- [x] Instalar dependências principais
- [x] Configurar estrutura de pastas
- [x] Configurar React Router com rotas básicas
- [x] Configurar Supabase client
- [x] Configurar ESLint e Prettier
- [x] Criar página inicial placeholder
- [x] Testar build de produção

---

## Dependências NPM

```json
{
  "dependencies": {
    "react": "^18.x",
    "react-dom": "^18.x",
    "react-router-dom": "^6.x",
    "@supabase/supabase-js": "^2.x",
    "@tanstack/react-query": "^5.x",
    "zustand": "^4.x",
    "@dnd-kit/core": "^6.x",
    "@dnd-kit/sortable": "^8.x",
    "lucide-react": "latest",
    "date-fns": "^3.x",
    "zod": "^3.x",
    "react-hook-form": "^7.x",
    "@hookform/resolvers": "^3.x",
    "clsx": "^2.x",
    "tailwind-merge": "^2.x"
  },
  "devDependencies": {
    "@types/react": "^18.x",
    "@types/react-dom": "^18.x",
    "typescript": "^5.x",
    "vite": "^5.x",
    "@vitejs/plugin-react": "^4.x",
    "tailwindcss": "^3.x",
    "postcss": "^8.x",
    "autoprefixer": "^10.x",
    "eslint": "^8.x",
    "prettier": "^3.x"
  }
}
```

---

## File List

```
frontend/
├── src/
│   ├── components/
│   │   ├── ui/           # shadcn components
│   │   └── layout/
│   │       └── Layout.tsx
│   ├── pages/
│   │   ├── Dashboard.tsx
│   │   └── NotFound.tsx
│   ├── hooks/
│   │   └── .gitkeep
│   ├── stores/
│   │   └── .gitkeep
│   ├── lib/
│   │   ├── supabase.ts
│   │   └── utils.ts
│   ├── types/
│   │   └── index.ts
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
├── public/
│   └── favicon.ico
├── .env.example
├── components.json         # shadcn config
├── tailwind.config.js
├── postcss.config.js
├── tsconfig.json
├── vite.config.ts
├── eslint.config.js
└── package.json
```

---

## Notas Técnicas

**Supabase Client (src/lib/supabase.ts):**
```typescript
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY

export const supabase = createClient(supabaseUrl, supabaseAnonKey)
```

**Variáveis de ambiente (.env.example):**
```env
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_URL=http://localhost:3001
```

---

## Definition of Done

- [x] Projeto React rodando com `npm run dev`
- [x] Tailwind CSS funcionando
- [x] shadcn/ui componentes disponíveis
- [x] Rotas básicas funcionando
- [x] Build de produção sem erros
- [x] ESLint sem warnings
